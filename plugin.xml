<?xml version="1.0" encoding="UTF-8"?>

<plugin xmlns="http://apache.org/cordova/ns/plugins/1.0" id="com.cordovaprogramming.meaningoflife" version="1.0.0">
    <name>MoLL</name>
    
    <description>Calculates the Meaning of Life (an obscure
        reference to Douglas Adams's Hitchhiker's Guide to the
        Galaxy)
    </description>
    
    <author>John M. Wargo</author>
    <keywords>mol, Meaning of Life</keywords>
    <license>Apache 2.0 License</license>
    <engines>
        <engine name="cordova" version="3.8.0" />
    </engines>
    
    // src ist name des plugin js; name ist unwichtig
    <js-module src="filename.js" name="moool">
        //clobbers sets namespace!
        // function doMOL() {
        // 		var res = mool.calculateMOL();
        <clobbers target="mool" />
    </js-module>
    <platform name="ios">
        <!-- ios-specific elements -->
        <header-file src="src/ios/NoisePlugin.h" />
        <source-file src="src/ios/NoisePlugin.m" />
        <config-file target="config.xml" parent="/*">
            <feature name="NoisePlugin" >
                <param name="ios-package" value="NoisePlugin"/>
            </feature> 
        </config-file>
    </platform>
    
    <info>This plugin exists for no other reason but to
        demonstrate how to build a simple, JavaScript-only
        plugin for Apache Cordova.</info> 
</plugin>